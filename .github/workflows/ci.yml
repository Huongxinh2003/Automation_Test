name: Java TestNG CI

on:
  push:
    branches: [ "main", "Test_1" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 📦 Build project
        run: mvn clean install

  run-signin-suite:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🧪 Run TestNG - SignIn Suite
        run: mvn test -DsuiteXmlFile=src/test/java/com/runSignIn.xml

      - name: 📊 Upload TestNG report (SignIn)
        uses: actions/upload-artifact@v4
        with:
          name: testng-signin-report
          path: target/surefire-reports/

  run-search-suite:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🧪 Run TestNG - Search Suite
        run: mvn test -DsuiteXmlFile=src/test/java/com/runSearch.xml

      - name: 📊 Upload TestNG report (Search)
        uses: actions/upload-artifact@v4
        with:
          name: testng-search-report
          path: target/surefire-reports/

  run-productdetail-suite:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🧪 Run TestNG - Product Detail Suite
        run: mvn test -DsuiteXmlFile=src/test/java/com/runProductDetail.xml

      - name: 📊 Upload TestNG report (Product Detail)
        uses: actions/upload-artifact@v4
        with:
          name: testng-productdetail-report
          path: target/surefire-reports/
